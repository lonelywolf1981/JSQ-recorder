<Window x:Class="JSQ.UI.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:JSQ.UI.WPF.ViewModels"
        mc:Ignorable="d"
        Title="JSQ Experiment Controller" Height="800" Width="1400"
        WindowState="Maximized"
        d:DataContext="{d:DesignInstance vm:MainViewModel}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="ChannelRowStyle" TargetType="DataGridRow">
            <Style.Triggers>
                <!-- данные идут, запись не ведётся → серая строка -->
                <DataTrigger Binding="{Binding Status}" Value="OK">
                    <Setter Property="Background" Value="#F5F5F5"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Warning">
                    <Setter Property="Background" Value="#FFF9C4"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Alarm">
                    <Setter Property="Background" Value="#FFCDD2"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="NoData">
                    <Setter Property="Background" Value="#F5F5F5"/>
                    <Setter Property="Foreground" Value="#9E9E9E"/>
                </DataTrigger>
                <!-- запись активна + OK → зелёная строка (перекрывает trigger выше) -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Status}" Value="OK"/>
                        <Condition Binding="{Binding IsRecording}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#F1F8F1"/>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatusDotStyle" TargetType="Ellipse">
            <Setter Property="Fill" Value="#BDBDBD"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Warning">
                    <Setter Property="Fill" Value="#FF9800"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Alarm">
                    <Setter Property="Fill" Value="#F44336"/>
                </DataTrigger>
                <!-- Зелёная точка только при активной записи + OK -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Status}" Value="OK"/>
                        <Condition Binding="{Binding IsRecording}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Fill" Value="#4CAF50"/>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="180"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Меню -->
        <Menu Grid.Row="0">
            <MenuItem Header="_Файл">
                <MenuItem Header="Вы_ход"/>
            </MenuItem>
            <MenuItem Header="_Настройки" Command="{Binding OpenSettingsCommand}"/>
            <MenuItem Header="_Помощь"/>
        </Menu>

        <!-- Панель инструментов -->
        <ToolBarTray Grid.Row="1">
            <ToolBar>
                <Button Content="⚙ Настройки" Command="{Binding OpenSettingsCommand}" Padding="10,5"
                        ToolTip="Недоступно во время записи"/>
            </ToolBar>
        </ToolBarTray>

        <!-- Основная область -->
        <Grid Grid.Row="2" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="340"/>
            </Grid.ColumnDefinitions>

            <!-- ===== Вкладки постов ===== -->
            <TabControl Grid.Column="0" Margin="0,0,5,0">

                <!-- Пост A -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="10" Height="10" Margin="0,0,5,0" VerticalAlignment="Center">
                                <Ellipse.Fill>
                                    <SolidColorBrush Color="{Binding PostA.StateBrush.Color}"/>
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock Text="Пост A"/>
                            <TextBlock Text=" (" Foreground="Gray"/>
                            <TextBlock Text="{Binding PostACount}" Foreground="Gray"/>
                            <TextBlock Text=")" Foreground="Gray"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Margin="3">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Панель управления постом A -->
                        <Border Grid.Row="0" Margin="0,0,0,4" Padding="6,4"
                                Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="1" CornerRadius="3">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Padding="8,3" CornerRadius="3" Margin="0,0,8,0">
                                    <Border.Background>
                                        <SolidColorBrush Color="{Binding PostA.StateBrush.Color}"/>
                                    </Border.Background>
                                    <TextBlock Text="{Binding PostA.StateBadge}"
                                               Foreground="White" FontWeight="Bold" FontSize="11"/>
                                </Border>

                                <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                           Text="{Binding PostA.CurrentExperiment.Name, TargetNullValue='—'}"
                                           FontStyle="Italic" Foreground="Gray" Margin="0,0,8,0"/>

                                <TextBlock Grid.Column="2" VerticalAlignment="Center"
                                           Text="{Binding PostA.AnomalyBadge}"
                                           Foreground="#E53935" FontWeight="Bold"
                                           Visibility="{Binding PostA.HasAnomalies, Converter={StaticResource BoolToVis}}"/>

                                <Button Grid.Column="3" Content="▶ Запуск"
                                        Command="{Binding StartPostCommand}" CommandParameter="A"
                                        IsEnabled="{Binding PostA.CanStart}"
                                        Foreground="Green" FontWeight="Bold"
                                        Margin="4,0,0,0" Padding="8,3"/>
                                <Button Grid.Column="4" Content="⏹ Стоп"
                                        Command="{Binding StopPostCommand}" CommandParameter="A"
                                        IsEnabled="{Binding PostA.CanStop}"
                                        Foreground="Red" FontWeight="Bold"
                                        Margin="4,0,0,0" Padding="8,3"/>
                                <Button Grid.Column="5" Content="📋 Каналы"
                                        Command="{Binding OpenChannelsForPostCommand}" CommandParameter="A"
                                        Margin="8,0,0,0" Padding="8,3"/>
                                <Button Grid.Column="6" Content="📊 История"
                                        Command="{Binding OpenEventHistoryCommand}" CommandParameter="A"
                                        Margin="4,0,0,0" Padding="8,3"/>
                            </Grid>
                        </Border>

                        <Grid Grid.Row="1" Margin="0,0,0,3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding PostASearch, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <DataGrid Grid.Row="2" ItemsSource="{Binding PostAFiltered}"
                                  AutoGenerateColumns="False" CanUserAddRows="False"
                                  IsReadOnly="True" VirtualizingPanel.IsVirtualizing="True"
                                  RowStyle="{StaticResource ChannelRowStyle}"
                                  MouseDoubleClick="ChannelDataGrid_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="" Width="20">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Ellipse Width="10" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                     Style="{StaticResource StatusDotStyle}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Канал"    Binding="{Binding ChannelName}"  Width="90"/>
                                <DataGridTextColumn Header="Значение"  Binding="{Binding CurrentValue, StringFormat=F3}" Width="90"/>
                                <DataGridTextColumn Header="Ед."       Binding="{Binding Unit}"          Width="50"/>
                                <DataGridTextColumn Header="Min"       Binding="{Binding MinLimit, StringFormat=F3}" Width="75"/>
                                <DataGridTextColumn Header="Max"       Binding="{Binding MaxLimit, StringFormat=F3}" Width="75"/>
                                <DataGridCheckBoxColumn Header="10s"   Binding="{Binding HighPrecision}" Width="45"/>
                                <DataGridTextColumn Header="Статус"    Binding="{Binding Status}"        Width="70"/>
                                <DataGridTextColumn Header="Обновлено" Binding="{Binding LastUpdateTime, StringFormat=HH:mm:ss}" Width="90"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>

                <!-- Пост B -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="10" Height="10" Margin="0,0,5,0" VerticalAlignment="Center">
                                <Ellipse.Fill>
                                    <SolidColorBrush Color="{Binding PostB.StateBrush.Color}"/>
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock Text="Пост B"/>
                            <TextBlock Text=" (" Foreground="Gray"/>
                            <TextBlock Text="{Binding PostBCount}" Foreground="Gray"/>
                            <TextBlock Text=")" Foreground="Gray"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Margin="3">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Margin="0,0,0,4" Padding="6,4"
                                Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="1" CornerRadius="3">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Padding="8,3" CornerRadius="3" Margin="0,0,8,0">
                                    <Border.Background>
                                        <SolidColorBrush Color="{Binding PostB.StateBrush.Color}"/>
                                    </Border.Background>
                                    <TextBlock Text="{Binding PostB.StateBadge}"
                                               Foreground="White" FontWeight="Bold" FontSize="11"/>
                                </Border>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                           Text="{Binding PostB.CurrentExperiment.Name, TargetNullValue='—'}"
                                           FontStyle="Italic" Foreground="Gray" Margin="0,0,8,0"/>
                                <TextBlock Grid.Column="2" VerticalAlignment="Center"
                                           Text="{Binding PostB.AnomalyBadge}"
                                           Foreground="#E53935" FontWeight="Bold"
                                           Visibility="{Binding PostB.HasAnomalies, Converter={StaticResource BoolToVis}}"/>
                                <Button Grid.Column="3" Content="▶ Запуск"
                                        Command="{Binding StartPostCommand}" CommandParameter="B"
                                        IsEnabled="{Binding PostB.CanStart}"
                                        Foreground="Green" FontWeight="Bold"
                                        Margin="4,0,0,0" Padding="8,3"/>
                                <Button Grid.Column="4" Content="⏹ Стоп"
                                        Command="{Binding StopPostCommand}" CommandParameter="B"
                                        IsEnabled="{Binding PostB.CanStop}"
                                        Foreground="Red" FontWeight="Bold"
                                        Margin="4,0,0,0" Padding="8,3"/>
                                <Button Grid.Column="5" Content="📋 Каналы"
                                        Command="{Binding OpenChannelsForPostCommand}" CommandParameter="B"
                                        Margin="8,0,0,0" Padding="8,3"/>
                                <Button Grid.Column="6" Content="📊 История"
                                        Command="{Binding OpenEventHistoryCommand}" CommandParameter="B"
                                        Margin="4,0,0,0" Padding="8,3"/>
                            </Grid>
                        </Border>

                        <Grid Grid.Row="1" Margin="0,0,0,3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding PostBSearch, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <DataGrid Grid.Row="2" ItemsSource="{Binding PostBFiltered}"
                                  AutoGenerateColumns="False" CanUserAddRows="False"
                                  IsReadOnly="True" VirtualizingPanel.IsVirtualizing="True"
                                  RowStyle="{StaticResource ChannelRowStyle}"
                                  MouseDoubleClick="ChannelDataGrid_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="" Width="20">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Ellipse Width="10" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                     Style="{StaticResource StatusDotStyle}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Канал"    Binding="{Binding ChannelName}"  Width="90"/>
                                <DataGridTextColumn Header="Значение"  Binding="{Binding CurrentValue, StringFormat=F3}" Width="90"/>
                                <DataGridTextColumn Header="Ед."       Binding="{Binding Unit}"          Width="50"/>
                                <DataGridTextColumn Header="Min"       Binding="{Binding MinLimit, StringFormat=F3}" Width="75"/>
                                <DataGridTextColumn Header="Max"       Binding="{Binding MaxLimit, StringFormat=F3}" Width="75"/>
                                <DataGridCheckBoxColumn Header="10s"   Binding="{Binding HighPrecision}" Width="45"/>
                                <DataGridTextColumn Header="Статус"    Binding="{Binding Status}"        Width="70"/>
                                <DataGridTextColumn Header="Обновлено" Binding="{Binding LastUpdateTime, StringFormat=HH:mm:ss}" Width="90"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>

                <!-- Пост C -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="10" Height="10" Margin="0,0,5,0" VerticalAlignment="Center">
                                <Ellipse.Fill>
                                    <SolidColorBrush Color="{Binding PostC.StateBrush.Color}"/>
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock Text="Пост C"/>
                            <TextBlock Text=" (" Foreground="Gray"/>
                            <TextBlock Text="{Binding PostCCount}" Foreground="Gray"/>
                            <TextBlock Text=")" Foreground="Gray"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Margin="3">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Margin="0,0,0,4" Padding="6,4"
                                Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="1" CornerRadius="3">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Padding="8,3" CornerRadius="3" Margin="0,0,8,0">
                                    <Border.Background>
                                        <SolidColorBrush Color="{Binding PostC.StateBrush.Color}"/>
                                    </Border.Background>
                                    <TextBlock Text="{Binding PostC.StateBadge}"
                                               Foreground="White" FontWeight="Bold" FontSize="11"/>
                                </Border>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                           Text="{Binding PostC.CurrentExperiment.Name, TargetNullValue='—'}"
                                           FontStyle="Italic" Foreground="Gray" Margin="0,0,8,0"/>
                                <TextBlock Grid.Column="2" VerticalAlignment="Center"
                                           Text="{Binding PostC.AnomalyBadge}"
                                           Foreground="#E53935" FontWeight="Bold"
                                           Visibility="{Binding PostC.HasAnomalies, Converter={StaticResource BoolToVis}}"/>
                                <Button Grid.Column="3" Content="▶ Запуск"
                                        Command="{Binding StartPostCommand}" CommandParameter="C"
                                        IsEnabled="{Binding PostC.CanStart}"
                                        Foreground="Green" FontWeight="Bold"
                                        Margin="4,0,0,0" Padding="8,3"/>
                                <Button Grid.Column="4" Content="⏹ Стоп"
                                        Command="{Binding StopPostCommand}" CommandParameter="C"
                                        IsEnabled="{Binding PostC.CanStop}"
                                        Foreground="Red" FontWeight="Bold"
                                        Margin="4,0,0,0" Padding="8,3"/>
                                <Button Grid.Column="5" Content="📋 Каналы"
                                        Command="{Binding OpenChannelsForPostCommand}" CommandParameter="C"
                                        Margin="8,0,0,0" Padding="8,3"/>
                                <Button Grid.Column="6" Content="📊 История"
                                        Command="{Binding OpenEventHistoryCommand}" CommandParameter="C"
                                        Margin="4,0,0,0" Padding="8,3"/>
                            </Grid>
                        </Border>

                        <Grid Grid.Row="1" Margin="0,0,0,3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding PostCSearch, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <DataGrid Grid.Row="2" ItemsSource="{Binding PostCFiltered}"
                                  AutoGenerateColumns="False" CanUserAddRows="False"
                                  IsReadOnly="True" VirtualizingPanel.IsVirtualizing="True"
                                  RowStyle="{StaticResource ChannelRowStyle}"
                                  MouseDoubleClick="ChannelDataGrid_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="" Width="20">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Ellipse Width="10" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                     Style="{StaticResource StatusDotStyle}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Канал"    Binding="{Binding ChannelName}"  Width="90"/>
                                <DataGridTextColumn Header="Значение"  Binding="{Binding CurrentValue, StringFormat=F3}" Width="90"/>
                                <DataGridTextColumn Header="Ед."       Binding="{Binding Unit}"          Width="50"/>
                                <DataGridTextColumn Header="Min"       Binding="{Binding MinLimit, StringFormat=F3}" Width="75"/>
                                <DataGridTextColumn Header="Max"       Binding="{Binding MaxLimit, StringFormat=F3}" Width="75"/>
                                <DataGridCheckBoxColumn Header="10s"   Binding="{Binding HighPrecision}" Width="45"/>
                                <DataGridTextColumn Header="Статус"    Binding="{Binding Status}"        Width="70"/>
                                <DataGridTextColumn Header="Обновлено" Binding="{Binding LastUpdateTime, StringFormat=HH:mm:ss}" Width="90"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>

            </TabControl>

            <!-- Панель информации (правая) -->
            <Grid Grid.Column="1" Margin="5,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Состояние постов -->
                <GroupBox Grid.Row="0" Header="Состояние постов" Margin="0,0,0,5">
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Row="0" Grid.Column="0" Width="8" Height="8" CornerRadius="4" Margin="0,0,6,0" VerticalAlignment="Center">
                            <Border.Background>
                                <SolidColorBrush Color="{Binding PostA.StateBrush.Color}"/>
                            </Border.Background>
                        </Border>
                        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Vertical" Margin="0,2">
                            <TextBlock FontWeight="Bold" Text="Пост A"/>
                            <TextBlock Text="{Binding PostA.CurrentExperiment.Name, TargetNullValue='Свободен'}"
                                       Foreground="Gray" FontSize="11"/>
                        </StackPanel>
                        <TextBlock Grid.Row="0" Grid.Column="2" VerticalAlignment="Center"
                                   Text="{Binding PostA.AnomalyBadge}" Foreground="#E53935"
                                   FontWeight="Bold" FontSize="11"/>

                        <Border Grid.Row="1" Grid.Column="0" Width="8" Height="8" CornerRadius="4" Margin="0,0,6,0" VerticalAlignment="Center">
                            <Border.Background>
                                <SolidColorBrush Color="{Binding PostB.StateBrush.Color}"/>
                            </Border.Background>
                        </Border>
                        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Vertical" Margin="0,2">
                            <TextBlock FontWeight="Bold" Text="Пост B"/>
                            <TextBlock Text="{Binding PostB.CurrentExperiment.Name, TargetNullValue='Свободен'}"
                                       Foreground="Gray" FontSize="11"/>
                        </StackPanel>
                        <TextBlock Grid.Row="1" Grid.Column="2" VerticalAlignment="Center"
                                   Text="{Binding PostB.AnomalyBadge}" Foreground="#E53935"
                                   FontWeight="Bold" FontSize="11"/>

                        <Border Grid.Row="2" Grid.Column="0" Width="8" Height="8" CornerRadius="4" Margin="0,0,6,0" VerticalAlignment="Center">
                            <Border.Background>
                                <SolidColorBrush Color="{Binding PostC.StateBrush.Color}"/>
                            </Border.Background>
                        </Border>
                        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Vertical" Margin="0,2">
                            <TextBlock FontWeight="Bold" Text="Пост C"/>
                            <TextBlock Text="{Binding PostC.CurrentExperiment.Name, TargetNullValue='Свободен'}"
                                       Foreground="Gray" FontSize="11"/>
                        </StackPanel>
                        <TextBlock Grid.Row="2" Grid.Column="2" VerticalAlignment="Center"
                                   Text="{Binding PostC.AnomalyBadge}" Foreground="#E53935"
                                   FontWeight="Bold" FontSize="11"/>
                    </Grid>
                </GroupBox>

                <!-- Состояние системы -->
                <GroupBox Grid.Row="1" Header="Здоровье системы" Margin="0,0,0,5">
                    <StackPanel Margin="5">
                        <Border BorderBrush="Gray" BorderThickness="1" Padding="10" CornerRadius="3">
                            <StackPanel>
                                <TextBlock FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"
                                           Text="{Binding SystemHealthStatusText}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="Gray"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SystemHealth.OverallStatus}" Value="OK">
                                                    <Setter Property="Foreground" Value="Green"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding SystemHealth.OverallStatus}" Value="Warning">
                                                    <Setter Property="Foreground" Value="Orange"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding SystemHealth.OverallStatus}" Value="Alarm">
                                                    <Setter Property="Foreground" Value="Red"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <Separator Margin="0,8"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Каналов:"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SystemHealth.TotalChannels}"
                                               HorizontalAlignment="Right" FontWeight="Bold"/>
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="OK:" Foreground="Green"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SystemHealth.ChannelsOK}"
                                               HorizontalAlignment="Right" Foreground="Green"/>
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Предупреждения:" Foreground="Orange"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SystemHealth.ChannelsWarning}"
                                               HorizontalAlignment="Right" Foreground="Orange"/>
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Отключены:" Foreground="Red"/>
                                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SystemHealth.ChannelsAlarm}"
                                               HorizontalAlignment="Right" Foreground="Red"/>
                                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Нет данных:" Foreground="Gray"/>
                                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SystemHealth.ChannelsNoData}"
                                               HorizontalAlignment="Right" Foreground="Gray"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                        <TextBlock Margin="0,8,0,0">
                            <Run Text="Samples/s: "/>
                            <Run Text="{Binding SystemHealth.SamplesPerSecond}"/>
                        </TextBlock>
                    </StackPanel>
                </GroupBox>

                <!-- Детальный статус -->
                <GroupBox Grid.Row="2" Header="Активные записи и события">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Активные посты с длительностью -->
                        <StackPanel Grid.Row="0" Margin="5,5,5,0">
                            <!-- Пост A -->
                            <Border Visibility="{Binding PostA.IsRunning, Converter={StaticResource BoolToVis}}"
                                    Background="#E8F5E9" BorderBrush="#4CAF50" BorderThickness="1"
                                    CornerRadius="3" Padding="6,4" Margin="0,0,0,3">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock FontWeight="Bold" FontSize="11">
                                            <Run Text="● Пост A:"/>
                                            <Run Text="{Binding PostA.CurrentExperiment.Name, Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock FontSize="11" Foreground="Gray">
                                            <Run Text="Длит.:"/>
                                            <Run Text="{Binding PostA.DurationDisplay, Mode=OneWay}"/>
                                        </TextBlock>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                               Text="{Binding PostA.AnomalyBadge, Mode=OneWay}"
                                               Foreground="#E53935" FontWeight="Bold" FontSize="12"/>
                                </Grid>
                            </Border>

                            <!-- Пост B -->
                            <Border Visibility="{Binding PostB.IsRunning, Converter={StaticResource BoolToVis}}"
                                    Background="#E8F5E9" BorderBrush="#4CAF50" BorderThickness="1"
                                    CornerRadius="3" Padding="6,4" Margin="0,0,0,3">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock FontWeight="Bold" FontSize="11">
                                            <Run Text="● Пост B:"/>
                                            <Run Text="{Binding PostB.CurrentExperiment.Name, Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock FontSize="11" Foreground="Gray">
                                            <Run Text="Длит.:"/>
                                            <Run Text="{Binding PostB.DurationDisplay, Mode=OneWay}"/>
                                        </TextBlock>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                               Text="{Binding PostB.AnomalyBadge, Mode=OneWay}"
                                               Foreground="#E53935" FontWeight="Bold" FontSize="12"/>
                                </Grid>
                            </Border>

                            <!-- Пост C -->
                            <Border Visibility="{Binding PostC.IsRunning, Converter={StaticResource BoolToVis}}"
                                    Background="#E8F5E9" BorderBrush="#4CAF50" BorderThickness="1"
                                    CornerRadius="3" Padding="6,4" Margin="0,0,0,3">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock FontWeight="Bold" FontSize="11">
                                            <Run Text="● Пост C:"/>
                                            <Run Text="{Binding PostC.CurrentExperiment.Name, Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock FontSize="11" Foreground="Gray">
                                            <Run Text="Длит.:"/>
                                            <Run Text="{Binding PostC.DurationDisplay, Mode=OneWay}"/>
                                        </TextBlock>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                               Text="{Binding PostC.AnomalyBadge, Mode=OneWay}"
                                               Foreground="#E53935" FontWeight="Bold" FontSize="12"/>
                                </Grid>
                            </Border>

                            <TextBlock Text="Нет активных записей" FontStyle="Italic" Foreground="Gray"
                                       FontSize="11" Margin="0,2">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsAnyPostRunning}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>

                        <!-- Последние аномалии -->
                        <GroupBox Grid.Row="1" Header="Последние события" Margin="0,4,0,0">
                            <ItemsControl ItemsSource="{Binding RecentAlerts}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,1" Padding="4,2" CornerRadius="2">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#FFF9C4"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Level}" Value="Error">
                                                            <Setter Property="Background" Value="#FFCDD2"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Level}" Value="Info">
                                                            <Setter Property="Background" Value="#E8F5E9"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <StackPanel>
                                                <TextBlock FontSize="10" Foreground="Gray">
                                                    <Run Text="{Binding Timestamp, StringFormat=HH:mm:ss, Mode=OneWay}"/>
                                                    <Run Text=" Пост"/>
                                                    <Run Text="{Binding Post, Mode=OneWay}"/>
                                                </TextBlock>
                                                <TextBlock FontSize="11" FontWeight="Bold"
                                                           Text="{Binding Source}" TextWrapping="Wrap"/>
                                                <TextBlock FontSize="11"
                                                           Text="{Binding Message}" TextWrapping="Wrap"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </GroupBox>
                    </Grid>
                </GroupBox>
            </Grid>
        </Grid>

        <!-- Лог событий -->
        <GroupBox Grid.Row="3" Header="Лог событий" Margin="5">
            <DataGrid ItemsSource="{Binding LogEntries}"
                      AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Время"    Binding="{Binding Timestamp, StringFormat=HH:mm:ss.fff}" Width="100"/>
                    <DataGridTextColumn Header="Уровень"  Binding="{Binding Level}"   Width="70"/>
                    <DataGridTextColumn Header="Пост"     Binding="{Binding Post}"     Width="50"/>
                    <DataGridTextColumn Header="Источник" Binding="{Binding Source}"   Width="90"/>
                    <DataGridTextColumn Header="Сообщение" Binding="{Binding Message}" Width="*"/>
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Level}" Value="Error">
                                <Setter Property="Background" Value="#FFCDD2"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Level}" Value="Warning">
                                <Setter Property="Background" Value="#FFF9C4"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Level}" Value="Info">
                                <Setter Property="Background" Value="#E8F5E9"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </GroupBox>

        <!-- Строка состояния -->
        <StatusBar Grid.Row="4">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock>
                    <Run Text="A:"/>
                    <Run Text="{Binding PostA.StateBadge, Mode=OneWay}"/>
                    <Run Text="  B:"/>
                    <Run Text="{Binding PostB.StateBadge, Mode=OneWay}"/>
                    <Run Text="  C:"/>
                    <Run Text="{Binding PostC.StateBadge, Mode=OneWay}"/>
                </TextBlock>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="JSQ v0.1.0"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
