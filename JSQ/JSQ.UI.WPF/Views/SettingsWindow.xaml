<Window x:Class="JSQ.UI.WPF.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:JSQ.UI.WPF.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Настройки" Width="500"
        SizeToContent="Height" MinHeight="320"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        d:DataContext="{d:DesignInstance vm:SettingsViewModel}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Передатчик -->
        <GroupBox Grid.Row="0" Header="Передатчик" Margin="0,0,0,8">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="IP-адрес:" VerticalAlignment="Center" Margin="0,4"/>
                <TextBox   Grid.Row="0" Grid.Column="1"
                           Text="{Binding TransmitterHost, UpdateSourceTrigger=PropertyChanged}"
                           Margin="0,4"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Порт:" VerticalAlignment="Center" Margin="0,4"/>
                <TextBox   Grid.Row="1" Grid.Column="1"
                           Text="{Binding TransmitterPort, UpdateSourceTrigger=PropertyChanged}"
                           Margin="0,4"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Таймаут (мс):" VerticalAlignment="Center" Margin="0,4"/>
                <TextBox   Grid.Row="2" Grid.Column="1"
                           Text="{Binding ConnectionTimeoutMs, UpdateSourceTrigger=PropertyChanged}"
                           Margin="0,4"/>
            </Grid>
        </GroupBox>

        <!-- Тест подключения -->
        <GroupBox Grid.Row="1" Header="Тест подключения" Margin="0,0,0,8">
            <StackPanel Margin="8,6">
                <StackPanel Orientation="Horizontal">
                    <Button Content="Проверить соединение"
                            Command="{Binding TestConnectionCommand}"
                            Padding="16,5" Margin="0,0,10,0"/>
                    <TextBlock Text="Проверяю..." VerticalAlignment="Center"
                               Foreground="Gray" FontStyle="Italic"
                               Visibility="{Binding IsTesting, Converter={StaticResource BoolToVis}}"/>
                </StackPanel>

                <!-- Результат теста -->
                <Border Margin="0,6,0,0" Padding="8,5" CornerRadius="3"
                        Visibility="{Binding TestResultVisible, Converter={StaticResource BoolToVis}}">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding TestResultSuccess}" Value="True">
                                    <Setter Property="Background" Value="#E8F5E9"/>
                                    <Setter Property="BorderBrush" Value="#4CAF50"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding TestResultSuccess}" Value="False">
                                    <Setter Property="Background" Value="#FFEBEE"/>
                                    <Setter Property="BorderBrush" Value="#F44336"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Text="{Binding TestResultMessage}" TextWrapping="Wrap">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding TestResultSuccess}" Value="True">
                                        <Setter Property="Foreground" Value="#2E7D32"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding TestResultSuccess}" Value="False">
                                        <Setter Property="Foreground" Value="#C62828"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Border>
            </StackPanel>
        </GroupBox>

        <!-- Пути -->
        <GroupBox Grid.Row="2" Header="Пути" Margin="0,0,0,8">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="База данных:" VerticalAlignment="Center" Margin="0,4"/>
                <TextBox   Grid.Row="0" Grid.Column="1"
                           Text="{Binding DatabasePath, UpdateSourceTrigger=PropertyChanged}"
                           Margin="0,4"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Путь экспорта:" VerticalAlignment="Center" Margin="0,4"/>
                <TextBox   Grid.Row="1" Grid.Column="1"
                           Text="{Binding ExportPath, UpdateSourceTrigger=PropertyChanged}"
                           Margin="0,4"/>
            </Grid>
        </GroupBox>

        <!-- Кнопки -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Command="{Binding SaveCommand}"
                    IsDefault="True" Padding="20,6" Margin="0,0,8,0"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}"
                    IsCancel="True" Padding="20,6"/>
        </StackPanel>
    </Grid>
</Window>
